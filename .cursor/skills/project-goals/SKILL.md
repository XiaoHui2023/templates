---
name: project-goals
description: templates 仓库标准五件套入口：记录未完成目标、证据与收敛条件。
---

# 项目目标

规则见 `~/.cursor/skills/agent-project-goals/SKILL.md`。

每个目标一个 `goals/<goal-id>.md` 文件。没有活动目标时，目录可以只保留 `.gitkeep`。
