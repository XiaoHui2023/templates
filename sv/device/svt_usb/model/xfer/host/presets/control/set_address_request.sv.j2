`ifndef {{ class_prefix | upper }}XFER_HOST_SET_ADDRESS_REQUEST
`define {{ class_prefix | upper }}XFER_HOST_SET_ADDRESS_REQUEST

class {{ class_prefix }}xfer_host_set_address_request extends {{ class_prefix }}xfer_host_control_request;
    `uvm_object_utils({{ class_prefix }}xfer_host_set_address_request)

    function new(string name = "{{ class_prefix }}xfer_host_set_address_request");
        super.new(name);
    endfunction

    constraint cst_set_address_preset {
        request == REQUEST_SET_ADDRESS;
        request_dir == REQUEST_DIR_HOST_TO_DEVICE;
        request_recipient == REQUEST_RECIPIENT_DEVICE;
        request_type == REQUEST_TYPE_STANDARD;
        w_index == 0;
        w_length == 0;
        w_value == device_addr;
        payload_intended_byte_count == 0;
    }
endclass

`endif
