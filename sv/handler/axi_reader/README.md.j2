{{ render('_axi_reader/README.md') }}

# 概要

将某种访问转化为 AXI 地址读取

# 开始的流程

根据配置生成访问地址，计算出相应的地址

输入数据来源于各个地址，然后送入内部的比较、显示和报告输出

# 目录结构

顶层可以访问输入、配置端口，也可以查看输出元素输入

如果配置不为有效访问，仍有来自数据的地址比较、表格输出和报表

# 使用方法

查看 router 的例子生成，链接 config 说明 compute 进行计算
