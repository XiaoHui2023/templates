`ifndef {{ (class_prefix ~ "base_item") | upper }}
`define {{ (class_prefix ~ "base_item") | upper }}

class {{ class_prefix }}base_item extends {{ class_prefix }}spec#(uvm_sequence_item);
    `uvm_object_utils({{ class_prefix }}base_item)

    function new(string name = "");
        super.new(name);
    endfunction
endclass

`endif
