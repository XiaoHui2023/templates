`ifndef {{ (class_prefix ~ "enable_clock_source_req") | upper }}
`define {{ (class_prefix ~ "enable_clock_source_req") | upper }}

class {{ class_prefix }}enable_clock_source_req extends uvm_sequence_item;
    `uvm_object_utils_begin({{ class_prefix }}enable_clock_source_req)
        `uvm_field_object(node, UVM_ALL_ON)
    `uvm_object_utils_end

    {{ class_prefix }}node_base node;

    function new(string name = "{{ class_prefix }}enable_clock_source_req");
        super.new(name);
    endfunction
endclass

`endif
