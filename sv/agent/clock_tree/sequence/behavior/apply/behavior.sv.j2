`ifndef {{ (class_prefix ~ "apply_seq") | upper }}
`define {{ (class_prefix ~ "apply_seq") | upper }}

class {{ class_prefix }}apply_seq extends uvm_sequence #(
    {{ class_prefix }}apply_req,
    {{ class_prefix }}apply_rsp
);
    `uvm_object_utils({{ class_prefix }}apply_seq)
    `uvm_declare_p_sequencer({{ class_prefix }}sequencer)

    function new(string name = "{{ class_prefix }}apply_seq");
        super.new(name);
    endfunction

    extern virtual task body();
endclass

task {{ class_prefix }}apply_seq::body();
    {{ class_prefix }}apply_req rq;
    {{ class_prefix }}apply_rsp rs;

    if (req == null)
        `uvm_fatal("CLK_TREE", "apply: req is null")

    rq = req;
    if (rq.tree == null)
        `uvm_fatal("CLK_TREE", "apply: req.tree is null")

    rs = {{ class_prefix }}apply_rsp::type_id::create("rsp");
    rs.ok = 1'b1;
    rsp = rs;
endtask

`endif
