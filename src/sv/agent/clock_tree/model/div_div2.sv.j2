`ifndef {{ (settings.class_prefix ~ "div_div2") | upper }}
`define {{ (settings.class_prefix ~ "div_div2") | upper }}

class {{ settings.class_prefix }}div_div2 extends {{ settings.class_prefix }}div_base;
    `uvm_object_utils({{ settings.class_prefix }}div_div2)

    constraint cst_div {
        ratio == 2;
{%- if enable_node_fix %}
        fix_ratio > 0 -> fix_ratio == 2;
{%- endif %}
    }

    function new(string name = "");
        super.new(name);
        div_kind = DIV2;
        ratio = 2;
    endfunction
endclass

`endif
