`ifndef {{ (class_prefix ~ "pll_tci") | upper }}
`define {{ (class_prefix ~ "pll_tci") | upper }}

class {{ class_prefix }}pll_tci extends {{ class_prefix }}base_pll;
    `uvm_object_utils({{ class_prefix }}pll_tci)
{%- for key in pll_reg_keys_by_kind['tci'] %}
    {{ class_prefix }}reg_bind f_{{ key }};
{%- endfor %}

    function new(string name = "");
        super.new(name);
{%- for key in pll_reg_keys_by_kind['tci'] %}
        f_{{ key }} = new();
{%- endfor %}
        pll_kind = PLL_TCI;
    endfunction
endclass

`endif
