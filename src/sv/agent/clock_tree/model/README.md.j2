{% if settings.direct_config -%}
# 时钟树

## 部署

输入 `{{ settings.class_regmodel }}` 寄存器为 `your-regmodel`。

```systemverilog
{{ settings.class_prefix }}tree tree;

// 例化 tree
tree = {{ settings.class_prefix }}tree::type_id::create("tree");
{% if regs_enabled %}
// 绑定寄存器
tree.build(your-regmodel);
{% else %}
// 随机化
tree.build();
{% endif -%}
```

{% if regs_enabled -%}
## 低功耗

关闭全部时钟以加速仿真，同时也会关闭你可能需要的时钟，不会关闭 PLL。

```systemverilog
// 尽可能关闭全部时钟
tree.low_power();
```

## 配置

灵活地开关时钟，或者修改频率。
{%- if first_clk_name %}

以 {{ first_clk_name }} 为例。
{% endif %}
```systemverilog
{% if first_clk_name -%}
// 使能目标时钟
tree.{{ first_clk_name }}.enabled = 1;

// 设置目标频率 123.456MHz
tree.{{ first_clk_name }}.frequence = 123456000;

{% endif -%}
{% if first_pll_access -%}
// 关闭 PLL
tree.{{ first_pll_access }}.enabled = 0;

{% endif -%}

// 应用配置
{{ settings.class_prefix }}config_reg(tree);
```
{% endif %}
{% if configurable_clks -%}
## 可配置时钟

| 时钟 | 推荐频率 |
| --- | --- |
{% for clk in configurable_clks -%}
| `{{ clk.name }}` | {% if not clk.active %}✗{% elif clk.freq is none %}-{% else %}{{ clk.freq_label }}{% endif %} |
{% endfor -%}
{% endif -%}
{% if configurable_plls -%}
## 可配置 PLL

| PLL | 推荐频率 |
| --- | --- |
{% for pll in configurable_plls -%}
| `{{ pll.name }}` | {{ pll.freq_label }} |
{% endfor -%}
{% endif -%}
{% endif -%}
