`ifndef {{ (class_prefix ~ "core_tools") | upper }}
`define {{ (class_prefix ~ "core_tools") | upper }}

class {{ class_prefix }}core_tools;
    {{ class_prefix }}reg_rw reg;
    {{ class_prefix }}node_cfg node;
    {{ class_prefix }}pll_cfg pll;

    function new();
        reg = new;
        node = new(reg);
        pll = new(reg);
    endfunction
endclass

`endif
