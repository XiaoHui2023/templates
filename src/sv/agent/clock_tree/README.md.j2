# 说明

## 外部约束

+ 你可以使用以下外部约束：
  + `cst_base`
  + `cst_user`
  + `cst_case`

```systemverilog
constraint {{ settings.class_prefix }}tree::cst_base {
    ...
}
constraint {{ settings.class_prefix }}tree::cst_user {
    ...
}
constraint {{ settings.class_prefix }}tree::cst_case {
    ...
}
```

## 建立与使用

在平台中建立 `{{ settings.class_prefix }}agent`，放入构建后的 `{{ settings.class_prefix }}tree`

```systemverilog
{{ settings.class_prefix }}agent clk_agt;
{{ settings.class_prefix }}kit_sequencer clk_sqr;
{{ settings.class_prefix }}tree tree;

function void build_phase(uvm_phase phase);
    ...
    clk_agt = {{ settings.class_prefix }}agent::type_id::create("clk_agt", this);
    tree = {{ settings.class_prefix }}tree::type_id::create("tree");
{%- if regs_enabled %}
    tree.build(your_regmodel);
{%- else %}
    tree.build();
{%- endif %}
    $cast(clk_sqr, clk_agt.sqr); // 取出工具方便使用
    uvm_config_db#({{ settings.class_prefix }}tree)::set(this, "clk_agt", "tree", tree);
endfunction
```

使用快捷函数

```systemverilog
task main_phase(uvm_phase phase);
    phase.raise_objection(this);
    ...
{%- if any_node_path and regs_enabled %}
    clk_sqr.test_measure();
{%- elif any_node_path %}
    clk_sqr.check_measure();
{%- elif regs_enabled %}
    clk_sqr.config_reg();
{%- endif %}
    ...
endtask
```
{% if first_clk_name and regs_enabled %}
## 配置时钟

开关时钟或改变频率，以 {{ first_clk_name }} 为例（详见 `tree.sv`）。

```systemverilog
task main_phase(uvm_phase phase);
    phase.raise_objection(this);
    
    tree.low_power(); // 低功耗（关闭全部时钟）
    tree.{{ first_clk_name }}.enabled = 1; // 开启时钟
    tree.{{ first_clk_name }}.frequence = 123456000; // 频率123.456Mhz
    clk_sqr.config_reg(); // 应用配置
endtask
```

{% endif %}

{% if any_node_path %}
## 顶层连线

在模块顶层例化 tree interface

```systemverilog
module your-top-module;
    ...
    {{ settings.class_prefix }}tree_interface clk_tree_if();
    ...
endmodule
```

在平台中连接 tree 与 interface

```systemverilog
function void build_phase(uvm_phase phase);
    ...
    your-top-module.clk_tree_if.connect_tree(tree);
endfunction
```

{% endif %}
