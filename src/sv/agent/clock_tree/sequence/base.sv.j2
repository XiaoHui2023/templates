`ifndef {{ (settings.class_prefix ~ "base_seq") | upper }}
`define {{ (settings.class_prefix ~ "base_seq") | upper }}

class {{ settings.class_prefix }}base_seq #(
    type REQ = uvm_sequence_item,
    type RSP = REQ
) extends uvm_sequence #(REQ, RSP);
    `uvm_object_param_utils({{ settings.class_prefix }}base_seq#(REQ, RSP))

{{ sv_family_declarations | indent(4, true) }}

    function new(string name = "{{ settings.class_prefix }}base_seq");
        super.new(name);
    endfunction
endclass

`endif
