`ifndef {{ (class_prefix ~ "check_clk_req") | upper }}
`define {{ (class_prefix ~ "check_clk_req") | upper }}

class {{ class_prefix }}check_clk_req extends uvm_sequence_item;
    `uvm_object_utils_begin({{ class_prefix }}check_clk_req)
        `uvm_field_object(tree, UVM_ALL_ON)
    `uvm_object_utils_end

    {{ class_prefix }}base_tree tree;

    function new(string name = "{{ class_prefix }}check_clk_req");
        super.new(name);
    endfunction
endclass

`endif
