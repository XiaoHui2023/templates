{% if regs_enabled %}
`ifndef {{ (class_prefix ~ "config_reg_req") | upper }}
`define {{ (class_prefix ~ "config_reg_req") | upper }}

class {{ class_prefix }}config_reg_req extends uvm_sequence_item;
    `uvm_object_utils_begin({{ class_prefix }}config_reg_req)
        `uvm_field_queue_object(nodes, UVM_ALL_ON | UVM_NOPRINT)
    `uvm_object_utils_end

    {{ class_prefix }}node_base nodes[$];

    function new(string name = "{{ class_prefix }}config_reg_req");
        super.new(name);
    endfunction
endclass

`endif
{% endif %}
