{% if any_node_path %}
`ifndef {{ (settings.class_prefix ~ "test_duty_wavefront_rsp") | upper }}
`define {{ (settings.class_prefix ~ "test_duty_wavefront_rsp") | upper }}

class {{ settings.class_prefix }}test_duty_wavefront_rsp extends uvm_sequence_item;
    `uvm_object_utils_begin({{ settings.class_prefix }}test_duty_wavefront_rsp)
        `uvm_field_int(ok, UVM_ALL_ON)
    `uvm_object_utils_end

    bit ok;

    function new(string name = "{{ settings.class_prefix }}test_duty_wavefront_rsp");
        super.new(name);
    endfunction
endclass

`endif
{% endif %}
