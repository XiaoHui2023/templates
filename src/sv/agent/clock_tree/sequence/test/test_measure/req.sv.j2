`ifndef {{ (settings.class_prefix ~ "test_measure_req") | upper }}
`define {{ (settings.class_prefix ~ "test_measure_req") | upper }}

class {{ settings.class_prefix }}test_measure_req extends {{ settings.class_prefix }}base_item;
    `uvm_object_utils_begin({{ settings.class_prefix }}test_measure_req)
        `uvm_field_object(tree, UVM_ALL_ON | UVM_NOPRINT)
        `uvm_field_int(quiet, UVM_ALL_ON)
        `uvm_field_int(debug, UVM_ALL_ON)
        `uvm_field_int(check_clk, UVM_ALL_ON)
        `uvm_field_int(check_cell, UVM_ALL_ON)
        `uvm_field_int(check_freq, UVM_ALL_ON)
        `uvm_field_int(check_duty, UVM_ALL_ON)
    `uvm_object_utils_end

    {{ settings.class_prefix }}tree_base tree;
    bit quiet;
    bit debug;
    bit check_clk;
    bit check_cell;
    bit check_freq;
    bit check_duty;

    function new(string name = "{{ settings.class_prefix }}test_measure_req");
        super.new(name);
        check_clk = 1'b1;
        check_cell = 1'b1;
        check_freq = 1'b1;
        check_duty = 1'b1;
    endfunction
endclass

`endif
