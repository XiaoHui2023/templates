`ifndef {{ class_prefix | upper }}BASE_OBJECT
`define {{ class_prefix | upper }}BASE_OBJECT

class {{ class_prefix }}base_object extends uvm_object;
    `uvm_object_utils({{ class_prefix }}base_object)

{{ sv_family_declarations | indent(4, true) }}

    function new(string name="{{ class_prefix }}base_object");
        super.new(name);
    endfunction
endclass

`endif
