`ifndef {{ (class_prefix ~ "interrupt_if") | upper }}
`define {{ (class_prefix ~ "interrupt_if") | upper }}

interface {{ class_prefix }}interrupt_if(input logic intr);
    timeunit 1ns;
    timeprecision 1fs;

    function bit is_connected();
        return !$isunknown(intr);
    endfunction

    function bit is_asserted();
        return is_connected() && intr;
    endfunction
endinterface

`endif
