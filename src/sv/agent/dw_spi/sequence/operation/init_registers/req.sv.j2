`ifndef {{ (settings.class_prefix ~ "init_registers_req") | upper }}
`define {{ (settings.class_prefix ~ "init_registers_req") | upper }}

class {{ settings.class_prefix }}init_registers_req extends uvm_sequence_item;
    `uvm_object_utils_begin({{ settings.class_prefix }}init_registers_req)
        `uvm_field_object(tr, UVM_ALL_ON)
    `uvm_object_utils_end

    rand {{ settings.class_prefix }}transfer tr;

    function new(string name="{{ settings.class_prefix }}init_registers_req");
        super.new(name);
    endfunction
endclass

`endif
