`ifndef {{ (settings.class_prefix ~ "flash_write_rsp") | upper }}
`define {{ (settings.class_prefix ~ "flash_write_rsp") | upper }}

class {{ settings.class_prefix }}flash_write_rsp extends uvm_sequence_item;
    `uvm_object_utils_begin({{ settings.class_prefix }}flash_write_rsp)
        `uvm_field_int(ok, UVM_ALL_ON)
        `uvm_field_int(bytes_written, UVM_ALL_ON | UVM_DEC)
    `uvm_object_utils_end

    bit ok;
    int unsigned bytes_written;

    function new(string name="{{ settings.class_prefix }}flash_write_rsp");
        super.new(name);
    endfunction
endclass

`endif
