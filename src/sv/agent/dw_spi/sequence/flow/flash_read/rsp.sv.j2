`ifndef {{ (class_prefix ~ "flash_read_rsp") | upper }}
`define {{ (class_prefix ~ "flash_read_rsp") | upper }}

class {{ class_prefix }}flash_read_rsp extends {{ class_prefix }}spec#(uvm_sequence_item);
    `uvm_object_utils_begin({{ class_prefix }}flash_read_rsp)
        `uvm_field_int(ok, UVM_ALL_ON)
        `uvm_field_queue_int(data, UVM_ALL_ON | UVM_HEX)
    `uvm_object_utils_end

    bit ok;
    bit [7:0] data[$];

    function new(string name="{{ class_prefix }}flash_read_rsp");
        super.new(name);
    endfunction
endclass

`endif
