`ifndef {{ (settings.class_prefix ~ "flash_read_rsp") | upper }}
`define {{ (settings.class_prefix ~ "flash_read_rsp") | upper }}

class {{ settings.class_prefix }}flash_read_rsp extends uvm_sequence_item;
    `uvm_object_utils_begin({{ settings.class_prefix }}flash_read_rsp)
        `uvm_field_int(ok, UVM_ALL_ON)
        `uvm_field_queue_int(data, UVM_ALL_ON | UVM_HEX)
    `uvm_object_utils_end

    bit ok;
    bit [7:0] data[$];

    function new(string name="{{ settings.class_prefix }}flash_read_rsp");
        super.new(name);
    endfunction
endclass

`endif
