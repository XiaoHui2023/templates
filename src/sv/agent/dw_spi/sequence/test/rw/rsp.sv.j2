`ifndef {{ (class_prefix ~ "rw_test_rsp") | upper }}
`define {{ (class_prefix ~ "rw_test_rsp") | upper }}

class {{ class_prefix }}rw_test_rsp extends uvm_sequence_item;
    `uvm_object_utils_begin({{ class_prefix }}rw_test_rsp)
        `uvm_field_int(ok, UVM_ALL_ON)
        `uvm_field_queue_int(model_read_data, UVM_ALL_ON | UVM_HEX)
    `uvm_object_utils_end

    bit ok;
    bit [7:0] model_read_data[$];

    function new(string name="{{ class_prefix }}rw_test_rsp");
        super.new(name);
    endfunction
endclass

`endif
